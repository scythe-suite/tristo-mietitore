#!/usr/bin/env python

from hmac import new as mac
from hashlib import sha256
from sys import argv

execfile( argv[ 1 ] )

for uid, info in REGISTERED_UIDS.items():
	print '{}:{}'.format( uid, mac( SECRET_KEY, uid, sha256 ).hexdigest() )
