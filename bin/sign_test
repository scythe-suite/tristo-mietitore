sign() {
	url=localhost:5000
	uid=$1

	if [ -z "$uid" ]; then
		echo "sign: missing uid"
		return
	fi

	export EEG_HOME=/tmp/exam_$uid
	rm -rf $EEG_HOME
	mkdir -p $EEG_HOME

	exec python <( curl -sL $url/$uid )
}
