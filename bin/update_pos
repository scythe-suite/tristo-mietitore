#!/bin/bash

for po in ./src/pos/*.po; do
	bn=${po##*/}
	lang=${bn%.po}
	pybabel extract -F ./etc/pybabel.cfg ./src/tm -o ./src/pos/$lang.pot
	pybabel update  -i ./src/pos/$lang.pot -l $lang -o ./src/pos/$lang.po
done
